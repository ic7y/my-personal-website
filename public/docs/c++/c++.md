# 面向对象程序设计

## 一、单项选择题

1. C++程序的入口函数是：（   ）

 A. start()    B. main()

 C. init()     D. begin()

2. 以下哪种数据类型占用4字节（32位系统）？（   ）

 A. Char       B. double

 C. Int         D. long long

3. ++i 和 i++ 的区别是：（   ）

 A. 无区别         B. ++i 先自增后取值，i++ 先取值后自增

 C. i++ 效率更高    D. 仅适用于整型

4. 以下代码的输出是：（   ）

int a = 10;  
int &b = a;  
b = 20;  
cout << a;  

 A. 10            B. 20

 C. 编译错误      D. 运行时错误

5. const int* p 的含义是：（   ）

 A. p 是常量指针       B. p 指向的值为常量

 C. p 的地址不可变     D. p 的值为常量

6. 以下代码的输出是：（   ）

for (int i = 0; i < 5; ++i) {  
    if (i == 3) break;  
    cout << i;  
}  

 A. 012                B. 01234

 C. 0123               D. 无输出

7. try-catch 块用于处理：（   ）

 A. 语法错误           B. 逻辑错误
 
 C. 运行时异常         D. 内存泄漏

8. 构造函数的作用是：（   ）

 A. 释放内存           B. 初始化对象

 C. 重载运算符         D. 销毁对象

9. 以下代码的输出是：（   ）

class A {  
public:  
    virtual void show() { cout << "A"; }  
};  
class B : public A {  
public:  
    void show() { cout << "B"; }  
};  
int main() {  
    A* obj = new B();  
    obj->show();  
}  

 A.  A                 B.  B

 C.  编译错误           D. 随机输出

10. 关于友元函数，正确的是：（   ）

 A. 可以访问类的私有成员         B. 是类的成员函数

 C. 不能被继承                   D. 必须通过对象调用

11. 以下代码的输出是：（   ）

class Test {  
public:  
    ~Test() { cout << "Destructor"; }  
};  
int main() {  
    Test t;  
}  

 A. 无输出                       B. Destructor

 C. 编译错误                     D. 运行时错误

12. 关于new和malloc的区别，正确的是：（   ）

 A. new是C语言的关键字         B. malloc会调用构造函数

 C. new返回void*类型            D. new会调用构造函数，malloc不会

13. Const expr关键字的作用是：（   ）

 A. 声明编译期常量或函数         B. 定义运行时变量

 C. 替代#define宏               D. 用于动态类型检查

14. 下列扩展名中，哪个是C++程序的头文件扩展名？（   ）

 A..h		B..cpp		C..c		D. .exe

15. C++标准输入/输出的头文件是（   ） 

 A.cmath	  B.iostream	    C.string		D.algorithm

16. 在C++中，使用new分配内存后，如果要释放空间，则应该使用（   ）运算符释放？

 A.free		B.delete	     C.auto		D.malloc

17. 下列关于文件的打开与关闭的说法中，错误的是（   ）。

 A.ifstream类.ostream类和fstream类都提供了成员函数open()用于打开文件。

 B.ifstream类打开文件的默认方式是ios::in，ostream类打开文件的默认方式是ios::out。

 C.文件流使用完毕后，析构函数会自动释放资源，不用手动调用close()函数关闭文件。

 D.文件的打开方式可以组合使用。

18. 关于C++异常处理的流程，下列说法中错误的是（   ）。

 A.对某段可能产生异常的代码或函数使用try结构进行检测

 B.如果在执行try结构期间没有引起异常，则跟在try后面的catch结构不会执行

 C.如果在执行try结构期间发生异常，在异常发生的位置使用throw抛出异常，异常对象将被创建

 D.本层try语句抛出了异常，只能由本层的catch语句处理

19. 下列选项中，哪一项不是C++11标准提供的智能指针（   ）

 A. unique_ptr	  B.shared_ptr	C.auto_ptr	   D.weak_ptr

20. 下列关于含有默认参数的函数说法中正确的是：（   ）

  A.在函数声明和函数定义中，默认参数的值必须保持一致

  B.如果一个函数中含有多个默认参数，则默认参数应从左至右依次定义

  C.当进行函数调用时，如果没有向带有默认值的参数传入实参，则使用默认值

  D.函数参数的默认值可以是全局变量，也可以是局部变量

21. 以下代码的输出是：(     )

for (int i = 0; i < 5; ++i) {  
    if (i == 3) break;  
    cout << i;  
}  

 A. 012            B. 01234

 C. 0123           D. 无输出

22. 类的默认访问权限是：(     )

 A. public     B. private

 C. protected  D. internal

23. 函数模板的实例化发生在：(     )

 A. 预处理阶段              B. 编译阶段

 C. 链接阶段                D. 运行时阶段

24. std::vector 的底层数据结构是：(     )
 
 A. 链表                    B. 动态数组

 C. 哈希表                  D. 树

25. 以下代码的输出是：(     )

std::vector<int> v{1, 2, 3};  

std::cout << v.size();  

 A. 0                       B. 3
 
 C. 6                       D. 编译错误

26. 以下代码的输出是：(     )

std::string s = "Hello";  
std::cout << s.substr(1, 3);  

 A. ell                    B. Hel
 C. ello                   D. H 

27. 下列选项中，哪一项不是C++11标准提供的智能指针(     )

 A. unique_ptr	   B.shared_ptr		C.auto_ptr		D.weak_ptr

28. 关于C++11多线程，下列说法中错误的是(     )

 A.C++11标准通过thread类模板创建多线程。

 B.在创建线程对象时，可以为线程传入参数。

 C.线程对象之间可以复制拷贝。

 D.detach()函数可以分离线程和线程对象。

29. 下列关于含有默认参数的函数说法中正确的是：(     )

  A.在函数声明和函数定义中，默认参数的值必须保持一致

  B.如果一个函数中含有多个默认参数，则默认参数应从左至右依次定义

  C.当进行函数调用时，如果没有向带有默认值的参数传入实参，则使用默认值

  D.函数参数的默认值可以是全局变量，也可以是局部变量

30. 当保护继承时,基类的(     )在派生类中成为保护成员,不能通过派生类的对象来直接访问。

  A.任何成员                 B.公有成员和保护成员

  C.公有成员和私有成员       D.私有成员

31. 下列关于指针的运算中，非法的是(     ) 

  A.两个指针在一定条件下可以进行相等或不相等的比较

  B.可以给一个指针赋空值

  C.可以用一个空指针赋值给别的指针

  D.两个指针在一定条件下可以相加

32. 下列程序的输出结果是(     )

#include<iostream>
using namespace std;
void main( ){ 
int a[2]={100,200};
int *p,*q;
p=q=a;
cout<<*++p<<“;”;
cout<<++*q<<endl;
}

 A.100，200      B.l01，200      C.200，100      D.200，101

33. 以下哪个声明表示指针本身为常量？(     )

 A. int* const p = &a;          B. const int* p = &a;

 C. const int* const p = &a;     D. int const* p = &a;

34. 对于int arr[5];，数组首元素的地址是？(     )

 A. Arr                      B. &arr[0]

 C. *arr                      D. A和B都正确

35. 结构体的大小由什么决定？(     )

 A. 所有成员大小之和                     B. 编译器根据对齐规则调整后的总大小

 C. 最大成员的大小                       D. 由程序员指定

36. 关于auto关键字，正确的是？(     )
 
 A. auto a 是合法声明                     B. auto可以推导void类型

 C. auto在编译时推导类型                 D.  auto不能用于Lambda参数

37. 关于nullptr，错误的是？(     )

 A. 替代NULL和0表示空指针            B. 可以隐式转换为任意指针类型

 C. 属于std命名空间                     D. 类型为std::nullptr_t

38. 关于friend关键字，正确的是？(     )

 A. 友元函数是类的成员函数               B. 友元类可以访问所有成员

 C. 友元关系不可继承                     D. 友元声明必须在类内部

39. 关于override关键字，正确的是？(     )

 A. 用于标记重载函数                     B. 确保派生类函数覆盖基类虚函数

 C. 仅用于抽象类                         D. 可省略不影响编译

40. 关于const关键字，错误的是？(     )

 A. const int a = 5; 表示a是常量         B. const成员函数不能修改类的成员变量

 C. const指针可以修改指向的地址       D. const对象只能调用const成员函数


41．关于关键字auto与decltype，下列说法中错误的是（ ）

 A、auto关键字用于推导变量类型。

 B、decltype关键字用于推导变量类型。

 C、可以使用auto关键字推导出的类型定义新的变量。

 D、decltype关键字的参数表达式不能是具体的数据类型。

42．关于nullptr，下列说法中错误的是（ ）

 A、nullptr是一个void*类型的指针。

 B、nullptr是一个有类型的空指针常量。

 C、nullptr不能转换为非指针类型。

 D、nullptr能够消除字面常量0带来的二义性。

43．关于=default和=delete关键字，下列说法中错误的是（ ）（多选）

 A、在类的默认构造函数后面添加=default，表示让编译器生成该函数的默认版本。

 B、使用=default修饰的函数需要实现。

 C、使用=delete修饰的成员函数，在类外不可以被调用。

 D、=delete不可以修饰普通函数，只能修饰类的成员函数。

44．下列选项中，哪一项不是C++11标准提供的智能指针（ ）

 A、unique_ptr	B、shared_ptr		C、auto_ptr		D、weak_ptr

45. 关于C++异常处理的流程，下列说法中错误的是（ ）。

 A、对某段可能产生异常的代码或函数使用try结构进行检测

 B、如果在执行try结构期间没有引起异常，则跟在try后面的catch结构不会执行

 C、如果在执行try结构期间发生异常，在异常发生的位置使用throw抛出异常，一个异常对象将被创建

 D、本层try语句抛出了异常，只能由本层的catch语句处理

46．关于类模板的模板参数，下列说法错误的是（）

 A、可以作为数据成员类型			

 B、可以作为成员函数的返回类型

 C、可以作为成员函数的参数类型	

 D、以上说法都正确

47．类模板的使用实际上是将类模板实例化成为一个（）

 A、函数			B、对象			C、类			D、抽象类

## 二、填空题

1. 声明一个整型变量a并初始化为10的语句是：______ a = 10;
2. 表达式 5 % 3 的结果是______。
3. 定义一个返回类型为void的无参函数foo的语句是：______ foo() {}
4. 使用______运算符可以获取变量的内存地址。
5. 函数参数传递时，使用______可以避免拷贝大型对象。
6. 函数重载的条件是：函数名相同，但参数______不同。
7. 类的默认访问权限是______。
8. 使用______运算符动态分配一个整型数组，大小为10。
9. 声明一个模板函数，模板参数名为T，返回类型为T的函数模板头是：template <______ T> T func();
10. std::vector中在末尾添加元素的方法是______。
11. 释放动态数组的内存应使用_______________运算符。
12. 右值引用的声明符号是_______________。
13. 使用____________关键字可以让编译器自动推导变量类型。
14. 现有Shape *p=new Shap[10]；假设要释放p指向的内存空间，那么释放内存的实现语句是______________________________。
15. 在编程中若要限制函数传入参数为只读，使用的关键字是_____________________________ 。
16. 类对象之间的赋值可以通过_________________________实现。
17. C++中的运算符可以重载为类的____     ____和_________________。
18. 在私有继承中，基类的公有成员在派生类中是__________________成员。
19. 虚函数使用_______________________关键字声明。
20. 关键字_________________可以检测派生类对基类虚函数的重写是否正确。
21. C++中，抛出异常的关键字是________。
22. C++标准异常库以________类为根基类。
23. C++标准异常类中，_______类表示运行时异常。
24. 在C++中，宏定义_______表示静态断言。
25. 在ios类库中，基类ios直接派生了两个类，分别是________和________。 
26. C++预定义的四个流对象中，不带缓冲的流对象是________。 
27. C++中的输入输出流可以分为标准I/O流、________和________三类。

## 三、判断题

1. (     )  C++中，main函数必须返回int类型。
2. (     )  const int a; 是合法的声明。
3. (     )  ++i和i++在独立使用时完全等价。
4. (     )  for (int i = 0; ; ) 是合法的无限循环。
5. (     )  try-catch块可以捕获所有类型的错误，包括语法错误。
6. (     )  构造函数可以声明为虚函数。
7. (     )  静态成员函数可以直接访问非静态成员变量。
8. (     )  纯虚函数可以有函数体。
9. (     )  友元函数是类的成员函数。
10. (     )  new和malloc都会调用构造函数。
11. (     ) 函数模板的实例化发生在运行时。
12. (     ) auto x = {1, 2, 3}; 推导出的类型是std::initializer_list<int>。
13. (     ) C++中的命名空间能够对作用域进行划分，避免命名冲突。
14. (     ) 使用函数重载时，若使用默认参数应当避免二义性出现。
15. (     ) C++中的引用是对一个对象起了别名，且必须初始化。
16. (     ) 在C++程序中，可以对枚举变量重新赋值。
17. (     ) string类可以使用STL模板库中的算法对字符串进行操作。
18. (     ) 析构函数必须要有返回值。
19. (     )  定义了构造函数之后，类不再提供默认的构造函数。
20. (     ) 类的常成员函数可以调用类的非常成员函数。
21.  try…catch语句中可以有多个try语句。
22.  try…catch语句可以分开，单独使用。
23.  try语句块中代码抛出异常后，如果异常被正确处理，抛出异常代码后面的程序会继续执行。 
24. 在C++流类库中，ios根基类是一个抽象类。 
25. ostream类提供的成员函数，如put()、write()函数等，无法连续调用。 
26. istream类提供的成员函数getline()，默认以‘\0’作为结束符。 
27.文件流对象无法使用>>、<<运算符传输数据。 5．C++不支持文件的随机读写。

## 四、程序填空题

1. 补全Person类的定义，使其包含姓名（name）和年龄（age）属性，并实现introduce 方法：

class Person {

private:
    
    ___________________; // 填空1：声明name（string类型）
    
    ___________________; // 填空2：声明age（int类型）

public:
    Person(string n, int a) : name(n), age(a) {}
    void introduce() {
        
	cout <<"我叫"<<name<<"，今年"<< ________ <<"岁"<< endl; // 填空3
    
	}
};
2. 补全代码，统计 Car 类实例的数量：
class Car {
    
	________ int count; // 填空4：声明静态成员

public:

    Car() { _________________; } // 填空5：增加count

    ~Car() { ________________; } // 填空6：减少count

    static int getCount() { return count; }

};

_____________________________________ // 填空7：初始化count

3. 补全单例模式的实现：
class Singleton {

    _______________ Singleton* instance; // 填空8：声明静态指针

    Singleton() {}

public:
    static Singleton* getInstance() {

        if (!instance) _________________; // 填空9：创建实例

        return instance;

    }
};

_________________________________________ // 填空10：初始化静态成员


4. 补全以下 Student 类的定义，包含构造函数.静态成员和友元函数：
#include <iostream>  
using namespace std;  
class Student {  
private:  
    string name;  
    int age;  

    ____________________; // 声明静态成员 totalStudents  

public:  

    Student(string n, int a) : ___________________ {  
        totalStudents++;  
    }  

    ___________________ { // 声明析构函数  
        totalStudents--;  
    }  
    static int getTotal() {  

        return ______________________;  

    }  
    friend void printStudent(const Student& s);  
};  

___________________ = 0; // 初始化静态成员  
void printStudent(const Student& s) {  

    cout << "Name: " << ___________________ << ", Age: " << ___________________;  

}  
int main() {  
    Student s1("Alice", 20);  
    printStudent(s1);  
    cout << "Total: " << Student::getTotal();  
}  

5. 补全代码，实现基类 Animal 和派生类 Dog 的动态绑定：
#include <iostream>  
using namespace std;  
class Animal {  

public:  

    ______________________ { // 声明虚函数  

        cout << "Animal speaks" << endl;  

    }  
};  

class Dog : public Animal {  

public:  

    ______________________ { // 重写虚函数  

        cout << "Dog barks" << endl;  

    }  
};  
int main() {  

    Animal* a = ______________________; // 创建Dog对象  

    a->speak();  

    ______________________; // 释放内存  

}  


## 五、简答题 

1. 简述C++中类模板和函数模板的作用。

2. 简述C++中虚函数的作用。

3. 简述一下在继承中，基类中不同访问权限的成员，在派生类中有什么变化。

4. 简述什么是函数重载，调用重载函数时要注意哪些问题。

5. 简述什么是运算符重载。

6. 简述一下C++中的多态。

7. 简述什么是模板。



## 六、编程题


1. 已知有一个若干元素的数组arr，使用函数模板求该数组的最大值。


2. 有一个文件data.txt，文件记录了几个学生的信息，内容如下：
姓名	学号	性别	年龄
张三	1001	男		22
李明	1002	男		21
李红	1003	女		22
李明	1004	女		20
请编写一个程序，从文件data.txt中找出所有叫李明的学生，并输出他们的信息。



3. 编写一个程序，实现复数类Complex，并重载”+”和”—”运算符，实现两个复数的和与差，并能够将结果输出到屏幕上。在main( )函数中进行下列计算。（15分）
 void main( )
{
  Complex com1(10,20),com2(5,30),com3;
  com3=com1+com2;
  com3=com1-com2;
  com3.disp( );
}




4. 请按照下列要求编写程序：
（1）定义一个异常类Cexception，有成员函数reason()，用来显示异常的类型。
（2）定义一个函数fun()触发异常，在主函数try语句块中调用fun()，在catch语句块中捕获异常，观察程序执行流程。




